---
description: Keep Checkmate living context updated on git commit when durable product/design truth changes.
alwaysApply: true
---

# Checkmate context maintenance

Repo: `/Users/madhurima/AI Repo/checkmate`

1. **Before work:** read `.cursor/skills/checkmate/SKILL.md` and `docs/CHECKMATE_CONTEXT.md`.
2. **Before commit:** if tokens, Figma nodes, strokes, dials, sync rules, architecture, or taste changed → update `docs/CHECKMATE_CONTEXT.md` (+ skill if workflow/taste rule changed) **in the same commit**; append Changelog line.
3. Ignore stale `~/.cursor/plans/checkmate_mvp_rebuild_*.plan.md`.

Skip for typos, formatting-only, or reverted debug.
